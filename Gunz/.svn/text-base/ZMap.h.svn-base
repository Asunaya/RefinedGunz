#ifndef _ZMAP_H
#define _ZMAP_H

#include "ZFilePath.h"
// 맵과 관련된 것들은 이 곳에 넣읍시다.


class MWidget;

// 현재 맵 폴더를 반환한다. - 퀘스트 맵은 패스가 다르기 때문에 만들어둔다.
void ZGetCurrMapPath(char* outPath);

// 해당위젯(콤보박스)에 게임가능한 맵을 추가한다.
bool InitMaps(MWidget *pWidget);


#endif